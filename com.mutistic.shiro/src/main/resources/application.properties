server.port=9088
#server.servlet.context-path=/xfrj

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/xfrj?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false&useInformationSchema=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.type=com.alibaba.druid.pool.DruidDataSource

spring.jpa.show-sql=true
#spring.jpa.hibernate.ddl-auto=update

spring.http.encoding.charset=utf-8
spring.http.encoding.enabled=true

## Redis配置信息
# Redis数据库索引（默认为0）
spring.redis.database=0
# Redis服务器地址
spring.redis.host=127.0.0.1
# Redis服务器连接端口
spring.redis.port=6379
# Redis服务器连接密码（默认为空）
spring.redis.password=
# Redis连接池最大连接数（使用负值表示没有限制）
spring.redis.jedis.pool.max-active=8
# Redis连接池最大阻塞等待时间（使用负值表示没有限制）
spring.redis.jedis.pool.max-wait=-1
# Redis连接池中的最大空闲连接
spring.redis.jedis.pool.max-idle=8
# Redis连接池中的最小空闲连接
spring.redis.jedis.pool.min-idle=0
# Redis连接超时时间（毫秒）：设置小于0会导致启动报错，设置为0会照成直接超时，与原本redis定义的timeout=0（永不过时）含义冲突，可以不设置此项，采用spring默认的超时时间
#spring.redis.timeout=1000000